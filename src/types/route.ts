export enum ClientRoute {
  ChatList = "/chat",
  Login = "/login",
}

export enum ApiRoute {
  Login = "/api/login",
  Register = "/api/register",
  AI = "/api/chats/ai",
  Me = "/api/me",
  Logout = "/api/logout",
  InitChat = "/api/chats",
}
